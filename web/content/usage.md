---
title: "Usage — @sebastienrousseau/ruby-config"
description: "How to use and configure @sebastienrousseau/ruby-config."
layout: "doc"
---

# Usage

`@sebastienrousseau/ruby-config` can be consumed across all standard module formats.

## CommonJS

```javascript
const config = require("@sebastienrousseau/ruby-config");
console.log(config.presets);
```

## ES Modules

```javascript
import config from "@sebastienrousseau/ruby-config";
console.log(config.defaultPreset);
```
